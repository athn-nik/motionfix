hydra:
  run:
    dir: .
  job:
    chdir: true
  output_subdir: null



folder: ???
mode: denoise # denoise / sample / sthing else

savedir: null
mean: false
fact: 1
number_of_samples: 1
ckpt_name: 'last'
last_ckpt_path: ${get_last_checkpoint:${folder},${ckpt_name}}
logger_level: INFO

steps: 100

defaults:
  - _self_
  - model/infer_scheduler: ddim
  - /path@path
  - override hydra/job_logging: rich # custom
  - override hydra/hydra_logging: rich # custom

